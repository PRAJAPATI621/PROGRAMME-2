#include <stdio.h>
int main()
{
 int a,b;
 printf("Enter 2 number\n");
 scanf("%d %d",&a,&b);
 f(a>b)
 {
  ptintf("%d is the larger number and %d is the smaller number among the number",a,b);
 }
 else
 {
  printf("both numbers are equal",a,b);
 }
}